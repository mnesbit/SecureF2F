pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version kotlin_version
    }
    repositories {
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'SecureF2F'
include 'crypto'
include 'avro'
include 'simpleactor'
include 'network'

