pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version "$kotlin_version"
    }
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'AkkaTest'
include 'crypto'
include 'avro'
include 'network'

